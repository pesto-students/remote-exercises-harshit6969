
// Don't change the export values.
// You can use the function in the Math module
const sqrt = (Number) => (Math.sqrt(Number));

const power = (Exponent, Number) => (Math.pow(Number, Exponent));

const round = (Number) => (Math.round(Number));

export {
  sqrt,
  power,
  round,
};
